2+2
setwd("~/Documents/GitHub/DataAnalyticsFall2022_Jody_Sunray/in-class/group1/week5")
setwd("~/Documents/GitHub/DataAnalyticsFall2022_Jody_Sunray/in-class/group2/week5")
# Decision Trees --
# Classification Tree
# Install the following libraries/packages
library(rpart)
library(rpart.plot)
# Decision Trees --
# Classification Tree
# Install the following libraries/packages
install.packages(rpart.plot)
# Decision Trees --
# Classification Tree
# Install the following libraries/packages
install.packages(rpart)
# Decision Trees --
# Classification Tree
# Install the following libraries/packages
install.package(rpart)
# Decision Trees --
# Classification Tree
# Install the following libraries/packages
install.packages(rpart)
# Decision Trees --
# Classification Tree
# Install the following libraries/packages
install.packages("rpart")
install.packages("rpart")
# Decision Trees --
# Classification Tree
# Install the following libraries/packages
install.packages("rpart.plot")
library(rpart)
library(rpart.plot)
iris
dim(iris)
# creating a sample from the iris dataset
s_iris <- sample(150,100)
s_iris
# create testing and training sets
iris_train <- isis[s_iris,]
# create testing and training sets
iris_train <- iris[s_iris,]
iris_test <- iris[-s_iris,]
dim(iris_train)
dim(iris_test)
help(sample)
# generate the decision tree model
decisionTreeModel <- rpart(Species~., iris_train, method = "class") # ~. includes remaining columns in data frame
decisionTreeModel <- rpart(Species~Sepal.Length+Sepal.Width+Petal.Length+Petal.Width, iris_train, method = "class")
# equivalent: decisionTreeModel <- rpart(Species~Sepal.Length+Sepal.Width+Petal.Length+Petal.Width, iris_train, method = "class")
decisionTreeModel
# plotting the decision tree model using
rpart.plot(decisionTreeModel)
